# Railway Configuration - Worker Service
# Celery Worker

[build]
builder = "DOCKERFILE"
dockerfilePath = "Api/Dockerfile"

[deploy]
startCommand = "/docker-entrypoint.sh worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
