# Railway Configuration - Frontend Service
# Flutter Web + Nginx

[build]
builder = "DOCKERFILE"
dockerfilePath = "Front/Dockerfile"
dockerContext = "Front"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
